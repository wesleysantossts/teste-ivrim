{
  "nodemonConfig": {
    "watch": ["src", ".env"],
    "ext": "js,ts,json",
    "ignore": ["src/**/*.spec.ts", "src/**/*.test.ts", ".git", "node_modules"],
    "exec": "ts-node -r dotenv/config -r tsconfig-paths/register --transpile-only src/app.ts"
  }
}